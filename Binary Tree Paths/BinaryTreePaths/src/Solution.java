import javax.swing.tree.TreeNode;
import java.util.*;
/**
 * Created by Administator on 2016/12/23.
 */
public class Solution {
    public List<String> binaryTreePaths(TreeNode root) {
        if (root == null)
            ls.add
    }
    public String helper(TreeNode root, List<String> ls) {
        StringBuffer sbf = new StringBuffer("" + root.val);
        if (root == null)
            ls.add(str);
        sbf += helper(root.left);
    }
}
